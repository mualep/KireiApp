# Legacy Pre-Freeze Plans

These files were created before PRD v1 freeze.

They are retained for reference only.

They must not be treated as source of truth.

The PRD freeze package under `docs/plans/` takes precedence.
